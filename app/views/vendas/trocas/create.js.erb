$("#itens_troca_table").prepend("<%= j render partial: 'vendas/trocas/item', object: @item %>");

$('#total_trocas_venda').html('<%= number_to_currency @venda.trocas.sum(:valor) %>');
$('#totais_pagamentos').html('<%= j render partial: "vendas/registros/totais" %>');

$(".bar-code-input").val('');

window.show_item_box({
  ref:   '<%= @item.produto.ref %>',
  cor:   '<%= @item.cor.try :nome %>',
  tam:   '<%= @item.tamanho.try :nome %>',
  title: 'Item DEVOLVIDO',
  focus: $('#barcode_troca')
});
