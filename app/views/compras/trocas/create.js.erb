$("#itens_troca_table").prepend("<%= j render partial: 'compras/trocas/item', object: @item %>");

$('#total_trocas_compra').html('<%= number_to_currency @compra.trocas.sum(:valor) %>');
$('#totais_pagamentos').html('<%= j render partial: "compras/registros/totais" %>');

$(".bar-code-input").val('');

window.show_item_box({
  ref:   '<%= @item.produto.descricao_short %>',
  cor:   '<%= @item.cor&.nome %>',
  tam:   '<%= @item.tamanho&.nome %>',
  title: 'Item DEVOLVIDO',
  focus: $('#barcode_troca')
});
