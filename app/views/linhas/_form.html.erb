<%= simple_form_for(@linha) do |f| %>

  <div class="row">
    <div class="col-md-6">
      <%= f.input :descricao, label: 'Descrição' %>

    </div>
  </div>

  <div class="bottom-actions">
    <%= f.button :submit, 'Salvar Linha', class: 'btn btn-primary' %>

    <%= link_to icn(:trash, 'Apagar este Linha'),
          @linha,
          method: :delete,
          data: { confirm: 'Tem certeza?' },
          class: 'btn btn-sm text-danger ml-auto' unless @linha.new_record? %>
  </div>

<% end %>
